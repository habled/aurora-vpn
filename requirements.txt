# aurora vpn — production dependencies.
# Pinned majors so a fresh install months from now still works.

aiogram>=3.4,<4.0          # Telegram bot client
aiohttp>=3.9,<4.0          # HTTP server (already a transitive of aiogram, listed explicitly)
asyncpg>=0.29,<1.0         # PostgreSQL client (async)
python-dotenv>=1.0,<2.0    # read .env for local development
